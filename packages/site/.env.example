VITE_GITHUB_USERNAME=fanfan-de

# Optional. The default endpoint reads public GitHub contribution data.
# Use {username} in a custom endpoint if the username should be interpolated.
# VITE_GITHUB_ACTIVITY_ENDPOINT=https://github-contributions-api.jogruber.de/v4/{username}?y=all

# Optional. Prefer a domestic CDN download manifest before GitHub Releases.
# VITE_DOWNLOAD_MANIFEST_URL=https://download.anybox.com.cn/downloads.json
