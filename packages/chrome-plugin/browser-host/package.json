{
  "name": "anybox-chrome-browser-host",
  "version": "0.14.0",
  "private": true,
  "description": "Plugin-owned Browser Host, policy gateway, and Chrome bridge.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json && node ./build.mjs",
    "test": "bun test ./Test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@anybox/chrome-shared": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "1.3.11",
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  }
}
