{
  "name": "anybox-chrome-native-host",
  "version": "0.12.0",
  "private": true,
  "description": "Rust Native Messaging host for the Anybox Chrome plugin.",
  "type": "module",
  "scripts": {
    "build": "node ./tools/build.mjs",
    "test": "cargo test --locked",
    "typecheck": "cargo check --locked"
  }
}
