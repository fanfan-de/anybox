name: Desktop technical-preview verification

on:
  pull_request:
  push:
    branches:
      - master
      - main

jobs:
  verify-desktop:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            builder_flag: --mac
            expected_arch: arm64
            plugin_test_pattern: installs immutable Release ZIPs|loads Chrome through
          - os: windows-2022
            builder_flag: --win
            expected_arch: x64
            plugin_test_pattern: installs immutable Release ZIPs|loads Chrome through|installs and removes Computer Use
          - os: ubuntu-22.04
            builder_flag: --linux
            expected_arch: x64
            plugin_test_pattern: installs immutable Release ZIPs

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify native runner architecture
        shell: bash
        run: node -e "if (process.arch !== '${{ matrix.expected_arch }}') throw new Error('Expected ${{ matrix.expected_arch }}, got ' + process.arch)"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Linux desktop build dependencies
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install --yes librsvg2-bin

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Verify product version
        run: pnpm verify:versions

      - name: Verify canonical plugin inventory and deterministic packaging
        if: matrix.os == 'ubuntu-22.04'
        run: pnpm plugins:index:check && pnpm plugins:release:test

      - name: Typecheck shared contracts
        run: pnpm --filter @anybox/shared typecheck

      - name: Typecheck platform adapter
        run: pnpm --filter @anybox/platform typecheck

      - name: Typecheck desktop
        run: pnpm --filter anybox-desktop-agent typecheck

      - name: Typecheck agent
        run: pnpm --filter anyboxagent exec tsc --noEmit -p tsconfig.json

      - name: Test agent shell and PTY runtime
        working-directory: packages/anyboxagent
        run: bun run test:shell

      - name: Test desktop
        run: pnpm --filter anybox-desktop-agent test

      - name: Test media runtime lock and release gate contract
        run: pnpm --filter anybox-desktop-agent test:media-runtime-lock

      - name: Test shared contracts
        run: pnpm --filter @anybox/shared test

      - name: Test platform adapter
        run: pnpm --filter @anybox/platform test

      - name: Test agent server
        working-directory: packages/anyboxagent
        run: bun test Test/server.api.test.ts

      - name: Test platform plugin installation
        working-directory: packages/anyboxagent
        run: bun test Test/plugin.test.ts --test-name-pattern "${{ matrix.plugin_test_pattern }}"

      - name: Package non-release Deliver technical preview directory
        run: pnpm --filter anybox-desktop-agent run dist:deliver-preview --dir ${{ matrix.builder_flag }}
